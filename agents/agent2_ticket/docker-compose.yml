services:
  agent2_ticket:
    build: .
    container_name: agent2_ticket
    ports:
      - "8002:8000"
    networks:
      - ai_network
    environment:
      - AGENT1_URL=http://agent1_student:8001

networks:
  ai_network:
    external: true
