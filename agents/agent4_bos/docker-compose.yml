services:
  agent4_bos:
    build: .
    container_name: agent4_bos
    ports:
      - "8004:8000"
    networks:
      - ai_network
    environment:
      - AGENT1_URL=http://agent1_student:8001

networks:
  ai_network:
    external: true

